<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>得獎須知</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
      background-color: #f4f4f4;
    }
    .container {
      max-width: 800px;
      margin: auto;
      padding: 20px;
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    h1 {
      font-size: 32px;
      margin-bottom: 20px;
      text-align: center;
      color: #007bff;
    }
    .tabs {
      display: flex;
      justify-content: center;
      cursor: pointer;
      margin-top: 20px;
    }
    .tab {
      padding: 10px 20px;
      background-color: #007bff;
      color: white;
      margin-right: 5px;
      border-radius: 5px;
      transition: background-color 0.3s;
    }
    .tab:hover {
      background-color: #0056b3;
    }
    .tab-content {
      display: none;
      margin-top: 20px;
      font-size: 18px;
      line-height: 1.6;
    }
    .tab-content.active {
      display: block;
    }
    .button {
      padding: 12px 20px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
      text-decoration: none;
      display: inline-block;
      margin-top: 20px;
    }
    .button:hover {
      background-color: #0056b3;
    }
  </style>
  <script>
    function showTabContent(tabId) {
      var contents = document.querySelectorAll('.tab-content');
      contents.forEach(function(content) {
        content.classList.remove('active');
      });
      document.getElementById(tabId).classList.add('active');
    }
    // 初始展示第一個tab
    document.addEventListener('DOMContentLoaded', function() {
      showTabContent('important-info');
    });
  </script>
</head>
<body>
  <div class="container">
    <h1>得獎須知</h1>
    <div class="tabs">
      <div class="tab" onclick="showTabContent('important-info')">領獎重要事項</div>
      <div class="tab" onclick="showTabContent('winner-list')">中獎名單</div>
    </div>
    <div id="important-info" class="tab-content active">
      <h2>領獎重要事項</h2>
      <p>這裡可以添加領獎重要事項的內容。</p>
    </div>
    <div id="winner-list" class="tab-content">
      <h2>中獎名單</h2>
      <p>這裡可以添加中獎名單的內容。</p>
    </div>
    <a href="/" class="button">返回主頁</a>
  </div>
</body>
</html>
