<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>第2天任務</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-color: #f4f4f4;
            padding: 20px;
        }

        .container {
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 100%;
        }

        h1 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        .question {
            margin-bottom: 20px;
        }

        .image-container {
            text-align: center;
            margin-bottom: 20px;
        }

        .image-container img {
            max-width: 100%;
            border-radius: 8px;
        }

        .button {
            padding: 10px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
        }

        .button:disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }

        .hidden {
            display: none;
        }

        .correct-answer {
            color: green;
            font-weight: bold;
        }

        .incorrect-answer {
            color: red;
            font-weight: bold;
        }

        #completionMessage {
            display: none;
        }

        #rewardMessage {
            color: red;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>第2天任務：圖文任務</h1>

        <!-- 第一題 -->
        <div id="question1" class="question">
            <p>問題1：這張圖片顯示的是哪個城市？</p>
            <div class="image-container">
                <img src="https://example.com/image1.jpg" alt="城市圖片">
            </div>
            <form id="form1">
                <input type="radio" name="q1" value="紐約" required> 紐約<br>
                <input type="radio" name="q1" value="倫敦"> 倫敦<br>
                <input type="radio" name="q1" value="東京"> 東京<br>
                <button type="submit" class="button">提交答案</button>
            </form>
            <p id="q1-correct" class="hidden correct-answer">正確答案：東京</p>
            <p id="q1-feedback"></p>
        </div>

        <!-- 第二題 -->
        <div id="question2" class="question hidden">
            <p>問題2：這張圖片中的建築是什麼？</p>
            <div class="image-container">
                <img src="https://example.com/image2.jpg" alt="建築圖片">
            </div>
            <form id="form2">
                <input type="radio" name="q2" value="艾菲爾鐵塔" required> 艾菲爾鐵塔<br>
                <input type="radio" name="q2" value="自由女神像"> 自由女神像<br>
                <input type="radio" name="q2" value="東京塔"> 東京塔<br>
                <button type="submit" class="button">提交答案</button>
            </form>
            <p id="q2-correct" class="hidden correct-answer">正確答案：自由女神像</p>
            <p id="q2-feedback"></p>
        </div>

        <!-- 第三題 -->
        <div id="question3" class="question hidden">
            <p>問題3：這張圖片是什麼時候拍攝的？</p>
            <div class="image-container">
                <img src="https://example.com/image3.jpg" alt="事件圖片">
            </div>
            <form id="form3">
                <input type="radio" name="q3" value="早上" required> 早上<br>
                <input type="radio" name="q3" value="下午"> 下午<br>
                <input type="radio" name="q3" value="晚上"> 晚上<br>
                <button type="submit" class="button">完成問答</button>
            </form>
            <p id="q3-correct" class="hidden correct-answer">正確答案：晚上</p>
            <p id="q3-feedback"></p>
        </div>

        <!-- 完成信息 -->
        <div id="completionMessage"
            class="<%= task.TaskDate === currentDate ? (isCompleted ? 'hidden' : '') : 'hidden' %>">
            <p>恭喜你完成了圖文任務！</p>
            <form id="completeForm" action="/task/2/complete" method="POST">
                <button type="submit"
                    class="<%= task.TaskDate === currentDate ? ( !isCompleted ? 'button' : 'hidden' ): 'hidden' %>"
                    id="completeButton">完成今日任務</button>
            </form>
            <p id="taskCompletedMessage" class="<%= isCompleted ? '' : 'hidden' %>">任務已完成</p>
        </div>
        <p id="notTodayMessage" class="<%= task.TaskDate === currentDate ? 'hidden' : '' %>" style="color: red;">非今日任務
        </p>
        <div id="rewardMessage" class="hidden"></div>
    </div>

    <script>
        document.getElementById('form1').addEventListener('submit', function (event) {
            event.preventDefault();
            document.getElementById('form1').style.display = 'none';
            const answer1 = document.querySelector('input[name="q1"]:checked').value;
            const correctAnswer1 = '東京';
            document.getElementById('q1-correct').classList.remove('hidden');
            document.getElementById('q1-feedback').textContent = answer1 === correctAnswer1 ? '回答正確！' : `回答錯誤。正確答案是：${correctAnswer1}`;
            document.getElementById('question2').classList.remove('hidden');
        });

        document.getElementById('form2').addEventListener('submit', function (event) {
            event.preventDefault();
            document.getElementById('form2').style.display = 'none';
            const answer2 = document.querySelector('input[name="q2"]:checked').value;
            const correctAnswer2 = '自由女神像';
            document.getElementById('q2-correct').classList.remove('hidden');
            document.getElementById('q2-feedback').textContent = answer2 === correctAnswer2 ? '回答正確！' : `回答錯誤。正確答案是：${correctAnswer2}`;
            document.getElementById('question3').classList.remove('hidden');
        });

        document.getElementById('form3').addEventListener('submit', function (event) {
            event.preventDefault();
            document.getElementById('form3').style.display = 'none';
            const answer3 = document.querySelector('input[name="q3"]:checked').value;
            const correctAnswer3 = '晚上';
            document.getElementById('q3-correct').classList.remove('hidden');
            document.getElementById('q3-feedback').textContent = answer3 === correctAnswer3 ? '回答正確！' : `回答錯誤。正確答案是：${correctAnswer3}`;
            document.getElementById('completionMessage').style.display = 'block';
        });

        document.getElementById('completeForm').addEventListener('submit', function (event) {
            event.preventDefault();
            // 使完成今日任務按鈕變灰且不可點選
            const completeButton = document.getElementById('completeButton');
            completeButton.disabled = true;
            completeButton.classList.add('button:disabled');
            const currentDate = new Date().toISOString().split('T')[0];
            const taskDate = '<%= task.TaskDate %>';
            if (currentDate !== taskDate) {
                alert('非今日任務');
            } else {
                fetch('/task/2/complete', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({})
                })
                    .then(response => response.json())
                    .then(data => {
                        const rewardMessage = document.getElementById('rewardMessage');
                        rewardMessage.textContent = `${data.message} 獲得 ${data.reward} 元的獎勵金。`;
                        rewardMessage.classList.remove('hidden');
                        setTimeout(() => {
                            rewardMessage.classList.add('hidden');
                            window.location.href = '/'; // 跳轉到首頁
                        }, 5000);
                    })
                    .catch(error => {
                        console.error('Error:', error);
                    });
            }
        });      
    </script>
</body>

</html>